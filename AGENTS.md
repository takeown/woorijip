# 우리집 저장소 작업 지침

## 제품 목표

우리집은 부부 두 사람이 사용하는 비공개 가계부다. 수동 입력을 최소화하고,
결제 정보를 안전하게 수집하며, 가계 지출을 이해하기 쉬운 말로 설명하는 것을
목표로 한다.

기능 작업을 시작하기 전에 `docs/PLAN.md`를 읽는다. 작업 범위가 달라지거나
마일스톤을 완료했다면 계획 문서를 갱신하고, 제품·기술 결정을 내렸다면
`docs/DECISIONS.md`에 기록한다.

## 저장소 구조

- `apps/web`: Next.js App Router, TypeScript, Tailwind CSS
- `apps/api`: Kotlin, Spring Boot, Spring Data JDBC, Flyway
- `compose.yaml`: 로컬 PostgreSQL
- `docs/PLAN.md`: 제품 범위, 현재·다음 마일스톤, 백로그
- `docs/MILESTONES_ARCHIVE.md`: 완료한 마일스톤 상세
- `docs/DECISIONS.md`: 제품·기술 결정과 배경
- `docs/ARCHITECTURE.md`: API 요청 흐름, 계층별 역할, 변경 지점, 장애 확인 순서
- `docs/SECURITY.md`: 보안 경계, 데이터 처리와 검증 기준

하나의 모노레포로 관리하지만 웹과 API는 독립적으로 배포한다.

API 구조를 설명하거나 변경 지점을 안내할 때는 `docs/ARCHITECTURE.md`를 기준으로 삼고,
구조가 달라졌다면 이 문서도 함께 갱신한다. 각 디렉터리의 `CLAUDE.md`는 같은 위치의
`AGENTS.md`를 가리키는 한 줄 파일이므로 지침은 `AGENTS.md`에만 작성한다.

## 작업 방식

- 작고 검토 가능한 vertical slice 단위로 작업한다.
- 현재 마일스톤을 충족하는 가장 단순한 구현을 우선한다.
- 가상의 미래 요구사항을 위한 추상화를 미리 만들지 않는다.
- 이 프로젝트는 Kotlin과 Spring 학습 목적도 있으므로, 이해하기 어려운 선택은
  작업 결과와 함께 한글로 설명한다.
- API 기능 작업 결과에는 초보자도 흐름을 따라갈 수 있도록 이번에 변경한
  Controller, Service, Repository, Flyway 등 Kotlin/Spring 구성요소의 역할을 설명한다.
- 작업 결과에는 주요 위험 요소, 실행한 테스트, 각 테스트가 보장하는 범위를
  구분해 알린다. 빌드 성공만으로 동작 전체가 검증됐다고 표현하지 않는다.
- 사용자와의 진행 상황 공유, 설명, 문서 작성은 기본적으로 한글을 사용한다.
- 소스 코드, 식별자, API 필드, 커밋 메시지는 영어를 사용한다.
- 사용자 화면에 표시되는 문구는 한글을 사용할 수 있다.
- 사용자가 명시적으로 요청하지 않으면 커밋하거나 push하지 않는다.

## 브랜치 규칙

- 브랜치 이름은 `<type>/<short-kebab-case-description>` 형식을 사용한다.
- 설명은 영어 kebab-case로 간결하게 작성한다.
- 허용하는 `type`은 `feat`, `fix`, `refactor`, `docs`, `chore`다.
- `feat`: 사용자 기능 추가 또는 변경
- `fix`: 버그 수정
- `refactor`: 동작 변경 없는 코드 구조 개선
- `docs`: 문서만 변경
- `chore`: 의존성, 설정, 빌드, CI 등 유지보수 작업

## 공통 보안 규칙

- Gmail 원문, OAuth 토큰, 인증 정보 및 비밀값을 로그나 커밋 파일에 저장하지 않는다.
- 필요한 최소 권한만 요청하고, 민감한 데이터는 필요한 기간만 보관한다.
- 예제 환경변수에는 실제 비밀값을 넣지 않는다.
- 외부 AI에는 기능 수행에 필요한 최소 데이터만 전송한다. 카드번호, 계좌번호,
  주민등록번호, OAuth 토큰과 인증 정보는 전송하지 않으며, 실명, 이메일과 내부 원본
  식별자도 꼭 필요한 경우가 아니면 전송하지 않는다.
- 사용자 입력에 외부 전송 금지 데이터가 포함될 수 있는 기능은 프롬프트나 문서 안내에만
  의존하지 않고 외부 요청 직전에 서버가 탐지해 거부하거나 제거한다.
- 외부 서비스에 전달하는 사용자 식별자는 원본 ID 대신 안정적인 HMAC 등으로
  가명화한다. 가명화 키는 코드나 데이터베이스와 분리해 비밀값으로 관리한다.
- 민감정보는 필요성이 확인되기 전까지 저장하지 않는다. 저장이 불가피하면 애플리케이션
  계층에서 암호화하고 키를 별도로 관리하며, 원문을 로그나 외부 AI 요청에 포함하지 않는다.
- 인증·권한, 외부 연동, 파일 업로드, 로그, 비밀값 또는 배포를 변경할 때는 작업 전에
  `docs/SECURITY.md`의 관련 항목을 읽고 필요한 음성 테스트를 추가한다.

## 명령어

별도 언급이 없으면 저장소 루트에서 실행한다.

```bash
pnpm install
docker compose up -d
pnpm dev:api
pnpm dev:web
```

검증 명령어:

```bash
pnpm lint:web
pnpm test:web
pnpm build:web
pnpm test:api
pnpm build:api
docker compose config -q
```

Node는 `.nvmrc`에 지정된 24 버전을 사용한다. 저장소에 포함된 Gradle Wrapper를
사용하므로 전역 Gradle이나 Kotlin 설치는 필요하지 않다.
`pnpm` 명령을 실행하기 전에는 반드시 `nvm use`로 `.nvmrc`의 Node 버전을 적용한다.

## 검증 기준

- 동작이 달라지면 관련 테스트를 추가하거나 수정한다.
- 개발 중에는 변경과 가장 가까운 테스트부터 실행한다.
- 작업을 넘기기 전에는 변경 범위에 해당하는 모든 검증을 실행한다.
- 실행하지 못한 검증이 있다면 이유와 함께 알린다.
- 호환성을 깨뜨리는 의존성 업데이트나 보안 자동 수정은 영향을 검토하고 설명하기
  전까지 강제로 적용하지 않는다.

## 계획 관리

- `docs/PLAN.md`에는 정확히 하나의 마일스톤만 `진행 중`으로 표시한다.
- 완료한 체크리스트를 갱신하고, 완료된 마일스톤 상세는
  `docs/MILESTONES_ARCHIVE.md`로 옮긴다.
- 구현이 계획과 달라졌거나 제품·기술 결정을 내렸다면 `docs/DECISIONS.md`에 기록한다.
- 마일스톤 범위를 조용히 확장하지 않는다. 후속 아이디어는 백로그에 기록한다.
- 각 마일스톤은 커밋할 수 있는 동작·테스트 가능한 상태로 끝낸다.
